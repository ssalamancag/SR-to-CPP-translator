global sizes
   # matrix dimension, default 10
   var N := 10
   # number of processes, default 2
   var PR := 2
   # strip size
   var S : bool
body sizes
   S := N/PR
   fa i := 1 to n ->
      write(i, "el factorial es ", fact(i))
   af
end