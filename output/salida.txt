#include <pthread.h>

using namespace std;

class sizes{
	public:
		int N = 10 ;
		int PR = 2 ;
		int S ;
		
}

class bodysizes(sizes sizes) {
	public:
		getarg 1 N ;
		getarg 2 PR ;
		S = N / PR ;
		if(N mod PR != 0 ){v("N must be a multiple of PR" )( 1 ) };
		if(M mod PR != 0 ){write("N must be a multiple of PR" )( 1 ) };
		
}

int main() {
	
}
